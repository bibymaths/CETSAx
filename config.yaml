# Paths
input_csv: "data/nadph.csv"
scripts_dir: "scripts"
results_dir: "results"

# Python Environment (Critical: Points to your UV venv)
python_bin: ".venv/bin/python"

# Training Hyperparameters
epochs: 10
batch_size: 8
task: "classification"
device: "cuda"  # Set to 'cpu' if needed